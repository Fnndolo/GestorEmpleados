# Veredicto de arquitectura â€” Plataforma TH/JurÃ­dica/SST KUPOCELL S.A.S.

LeÃ­ el documento de requerimientos (`C:/Users/EQUIPO/.claude/plans/en-la-raiz-de-swirling-wombat.md`) y los 4 diseÃ±os. A continuaciÃ³n: conflictos resueltos, vacÃ­os, orden de implementaciÃ³n y riesgos.

---

## 1. CONFLICTOS DETECTADOS Y VEREDICTO ÃšNICO

| # | Conflicto | DiseÃ±os | Veredicto |
|---|---|---|---|
| C1 | **Idioma del esquema**: espaÃ±ol (D1: `Colaborador`, `Vencimiento`) vs inglÃ©s (D3: `Deadline`, `AlertRule`) vs mixto (D4: `Empleado`, `User`) | 1Â·3Â·4 | **EspaÃ±ol** (convenciÃ³n D1: PascalCase â†’ snake_case). El dominio jurÃ­dico-laboral colombiano no se traduce sin ambigÃ¼edad. Ãšnica excepciÃ³n: las tablas que genera el adaptador de Better Auth (`User`, `Session`, `Account`, `Verification`) conservan su nombre. Todo el motor de alertas de D3 se renombra: `Deadlineâ†’Vencimiento`, `DeadlineTypeâ†’TipoVencimiento`, `DeadlineAlertâ†’AlertaVencimiento`, `AlertRuleâ†’ReglaAlerta`, `DeadlineResponsibleâ†’ResponsableVencimiento`, `OutboundMessageâ†’MensajeSaliente`, `LegalObligationOccurrenceâ†’OcurrenciaObligacion`. |
| C2 | **AutenticaciÃ³n**: Supabase Auth (D1: `Usuario.authId â†’ auth.users`) vs Better Auth (D4) | 1Â·4 | **Better Auth** (adaptador Prisma, plugin `admin`, `disableSignUp`, invitaciÃ³n por Resend). Razones de D4 son sÃ³lidas: usuarios en el mismo schema Prisma (FK directa a `Colaborador`, auditables por la misma extensiÃ³n), creaciÃ³n solo por admin, sin sincronizar `auth.users`. Supabase queda **solo** como Postgres + Storage. Se elimina `Usuario.authId`; el `User` de Better Auth se extiende con `additionalFields`: `rolId`, `estado`, `debeCambiarPassword`, `colaboradorId`, `telefonoE164`, `whatsappOptIn`. |
| C3 | **PKs**: `uuid(7)` (D1) vs `gen_random_uuid()` v4 (D3) vs `cuid()` (D4); `AuditLog` BigInt autoincrement (D4) | 1Â·3Â·4 | **UUIDv7 en todos los modelos**, incluido `AuditLog` (con Ã­ndice BRIN sobre `creado_en` de D1). Ordenado en el tiempo, amigable con B-tree, una sola convenciÃ³n. Requiere Prisma â‰¥ 5.19. |
| C4 | **Nombre de la persona**: `Colaborador` (D1) vs `Empleado/empleadoId` (D2, D4) | 1Â·2Â·4 | **`Colaborador`**: cubre contratistas OPS, aprendices y practicantes, donde "empleado" es jurÃ­dicamente incorrecto. Las rutas UI siguen siendo `/personas` (D4). |
| C5 | **Arquitectura del motor de alertas**: D1 calcula en el cron sobre `Vencimiento` con estados; D3 materializa pasos (`DeadlineAlert`) + reglas configurables + outbox + claim `FOR UPDATE SKIP LOCKED`; D4 un escaneo simple | 1Â·3Â·4 | **Arquitectura D3 con nombres D1** (ver C1). La materializaciÃ³n de pasos con `@@unique(vencimientoId, stepKey)`, el claim con `SKIP LOCKED` y el `dedupeKey` en notificaciÃ³n/outbox dan idempotencia estructural que D1 y D4 no tienen. Se adopta tambiÃ©n `ReglaAlerta` configurable por tipo (cubre el doble esquema 10H/3 global y 5H/1 del calendario legal sin hardcodear) y `ResponsableVencimiento` (usuario y/o rol, principal/copia) en lugar del `responsableId` Ãºnico de D1. `catch-up` automÃ¡tico (`scheduled_for <= hoy`) si el cron falla un dÃ­a. |
| C6 | **Festivos Colombia â€” 4 soluciones distintas**: `festivos-colombianos` (D1), implementaciÃ³n propia o `date-holidays` (D2), `colombian-holidays` verificada (D3), lib propia (D4) | todos | **`colombian-holidays` (^5.x)** â€” es la Ãºnica que un diseÃ±ador verificÃ³ contra npm/GitHub (API, Emiliani, `celebrationDate`). Se encapsula en **un Ãºnico** `lib/dias-habiles.ts` + tabla `FestivoExcepcion` (HolidayOverride de D3: ADD/REMOVE) + setting `excluirSabado` (default false: sÃ¡bado es hÃ¡bil). **Lo consumen los tres dominios**: motor de alertas (dÃ­as hÃ¡biles), clasificador de horas de nÃ³mina (dominical/festivo) y plazos jurÃ­dicos (RNBD 10 hÃ¡biles, garantÃ­as 15 hÃ¡biles). Se elimina la tabla `DiaFestivo` seed de D1 (mantener dos fuentes invita a desincronizaciÃ³n). Test unitario obligatorio del traslado Emiliani (6-ene-2026 martes â†’ celebraciÃ³n lunes 12). |
| C7 | **ParÃ¡metros legales**: `ParametroLegalAnual` (fila por aÃ±o, columnas tipadas, D1) vs `ParametroLegal` clave/valor con `vigenciaDesde/Hasta` (D2) | 1Â·2 | **Esquema D2 (clave/valor con vigencias)**. Los cambios legales NO se alinean al aÃ±o calendario (divisor horario cambia el 15-jul, franja nocturna el 25-dic, recargo dominical el 1-jul, SMMLV 2026 puede cambiar retroactivamente por el fallo del Consejo de Estado). D1 lo reconoce implÃ­citamente al necesitar los parches `RecargoVigencia` y `JornadaLaboralVigencia` â€” ambos se **eliminan** y se absorben como claves con vigencia (`RECARGO_DOMINICAL`, `DIVISOR_HORA`, `HORA_INICIO_NOCTURNA`). Se conservan las tablas especializadas de D2: `TarifaArl` (con vigencias), `RangoFsp`, `TramoRetefuente`, `TipoHora`. Campo `fuenteLegal` obligatorio (se imprime en reportes). ResoluciÃ³n con `resolverParametro(clave, fechaDelDÃ­aLiquidado)` y error bloqueante si no hay vigencia (nunca defaults silenciosos). |
| C8 | **Registro de horas extra**: usuario digita tipo+horas (`RegistroHorasExtra`, D1) vs rangos horarios + clasificador automÃ¡tico (`NovedadHoras`, D2) | 1Â·2 | **D2: rangos + clasificaciÃ³n automÃ¡tica** (HED/HEN/RN/RD/RND/HEDD/HEND derivados de franja nocturna vigente + festivos + jornada). Es lo que exige la carga probatoria de la Ley 2466 y elimina el error humano en la matriz de 7 tipos Ã— 4 vigencias. El tramo clasificado es editable con auditorÃ­a (ajuste manual excepcional). ValidaciÃ³n blanda del tope 2 HE/dÃ­a, 12/semana. |
| C9 | **Tablas de nÃ³mina**: `PeriodoNomina/LiquidacionNomina/DetalleNomina` (D1) vs `NominaPeriodo/NominaDetalle/NominaConceptoValor` (D2); estados distintos | 1Â·2 | **Nombres D1 + semÃ¡ntica D2**: `PeriodoNomina` gana `parametrosSnapshot Json` (congelado al cerrar) y estados `BORRADORâ†’CALCULADAâ†’APROBADAâ†’CERRADAâ†’PAGADA`; `DetalleNomina` gana columnas `base` y `factor` (trazabilidad del desprendible) ademÃ¡s de `cantidad/valorUnitario/valor` y el origen polimÃ³rfico de D1. Periodos cerrados son inmutables; las correcciones generan **periodo de ajuste** (D2). |
| C10 | **Flags de `ConceptoNomina`**: D1 (`baseSeguridadSocial`, `baseParafiscales`, `basePrestaciones`) vs D2 (`afectaIbcSs`, `basePrestaciones`, `baseVacaciones`, `afectaRetefuente`, `prorrateaPorDias`, fÃ³rmulas `EXPRESION`) | 1Â·2 | **UniÃ³n con base D2**: `constitutivoSalario`, `afectaIbcSs`, `basePrestaciones`, `baseVacaciones` (distinciÃ³n legal real: HE entra a prestaciones pero no a vacaciones), `afectaRetefuente`, `prorrateaPorDias`. Se **elimina** `baseParafiscales` de D1 (parafiscales se liquidan sobre el mismo IBC â†’ redundante). `tipoCalculo`: `VALOR_FIJO | PORCENTAJE_BASE | CANTIDAD_POR_VALOR | EXPRESION | SISTEMA`; fÃ³rmulas `EXPRESION` con mathjs en instancia restringida solo para conceptos creados por admin; los `SISTEMA` viven en TypeScript versionado. Regla Ley 1393 (tope 40% no constitutivo â†’ `AJUSTE_IBC_L1393`) de D2 se adopta. |
| C11 | **Cuentas de cobro OPS**: campos SS embebidos en `CuentaCobroOps` + CHECK (D1) vs tabla `SoporteSsOps` separada + estado `BLOQUEADA_SS` (D2) | 1Â·2 | **Merge**: `CuentaCobroOps` + tabla hija `SoporteSsOps` (permite correcciones/re-radicaciÃ³n y guarda operador, periodo cotizado, IBC declarado, verificador). Estados D1 + `BLOQUEADA_SS` de D2. Se conserva el **CHECK de D1** adaptado: imposible `APROBADA/PAGADA` sin soporte con `estadoVerificacion=VALIDA` (defensa en BD ademÃ¡s de la Server Action). Regla IBC â‰¥ 40% del mensualizado (Ley 1955 art. 244) con tolerancia 1%. |
| C12 | **PILA**: D1 dice "el archivo PILA se genera desde estas tablas"; D2 demuestra que el plano Res. 2388 no es viable en v1 y propone Resumen Excel/CSV | 1Â·2 | **D2**: v1 = "Resumen PILA" Excel/CSV mapeado 1:1 a campos del operador + totales de control; v2 = plano nativo detrÃ¡s de interfaz `PilaExporter`. Se conserva `PlanillaPila` de D1 para registrar pago mensual y su `Vencimiento`. **Comunicar explÃ­citamente este alcance al cliente** (gestiÃ³n de expectativa). |
| C13 | **RBAC**: alcance a nivel de `Rol` + catÃ¡logo `Permiso` con enums PG (D1) vs alcance por permiso + `modulo` String + `custom:{slug}` (D4); 9 roles (D1) vs 7 (D4) | 1Â·4 | **Alcance por permiso** (D4 â€” necesario: el Jefe de Ã¡rea ve personas con alcance EQUIPO pero el Contador ve nÃ³mina TODAS y SST nada), con los **valores de alcance de D1**: `TODAS_SEDES | SEDES_ASIGNADAS | EQUIPO | PROPIO` + tabla `UsuarioSede` (un RRHH regional necesita N sedes; el `SEDE` Ãºnica de D4 no alcanza). `modulo` como **String validado contra constantes tipadas** (no enum PG): los mÃ³dulos personalizados requieren `custom:{slug}` dinÃ¡mico, imposible con el enum `ModuloSistema` de D1. **9 roles seed** (los 7 de D4 + JurÃ­dica + Responsable SST de D1: ambos mÃ³dulos enteros necesitan operador), todos `esSistema=true` con matriz editable. Esto ademÃ¡s **elimina la colisiÃ³n interna de D1**: tenÃ­a dos `model Permiso` (RBAC Â§1 y novedad laboral Â§5) â€” el de RBAC desaparece (solo queda `RolPermiso`) y `Permiso` queda Ãºnicamente como la novedad laboral. |
| C14 | **Datos sensibles de salud**: campos inline en `Colaborador` + permisos por campo (D1) vs modelo `EmpleadoSalud` 1:1 separado (D4) | 1Â·4 | **HÃ­brido**: las **afiliaciones** (EPS/AFP/fondo cesantÃ­as/caja/ARL) se quedan en `Colaborador` â€” las necesita nÃ³mina/PILA a diario y no son dato sensible en sÃ­ mismas. Lo **clÃ­nico** (CIE-10 de `Incapacidad`, `concepto/recomendaciones/restricciones` de `ExamenMedico`, `RecomendacionMedica`) ya vive en modelos propios de SST: se protege con permiso `personas.salud` (nunca `include` sin `requirePermission`), `nivelAcceso=SST_MEDICO` en documentos, y redacciÃ³n en el diff de auditorÃ­a. No se crea `EmpleadoSalud` (duplicarÃ­a FKs que nÃ³mina consulta en cada liquidaciÃ³n). |
| C15 | **Flujos de aprobaciÃ³n**: N pasos configurables (`FlujoAprobacion/PasoFlujoAprobacion`, D1) vs mÃ¡quina fija de 2 niveles + `FlujoConfig.requiereNivelJefe` (D4) | 1Â·4 | **D1 (N pasos configurables)** â€” subsume a D4 y satisface el requisito "config niveles de aprobaciÃ³n por tipo". El seed reproduce exactamente la semÃ¡ntica de D4: paso 1 jefe inmediato (opcional por tipo, `usaJefeInmediato`), paso 2 RRHH **o** Subgerencia (cualquiera resuelve). Reglas de D4 que se conservan: RRHH/Admin pueden saltar el nivel 1 dejando constancia; transiciÃ³n + efectos en una transacciÃ³n; cancelaciÃ³n por el solicitante. |
| C16 | **MÃ³dulos personalizados**: D1 (`llevaSede`, tipos ricos, `generaAlerta` por campo FECHA, validaciÃ³n zod dinÃ¡mica) vs D4 (`seccion` de menÃº, `vinculo GLOBAL/POR_EMPLEADO/POR_SEDE`, permisos `custom:{slug}`, tipo `FECHA_VENCIMIENTO` aparte, dnd-kit) | 1Â·4 | **Merge**: tipos de campo de D1 (incluye MONEDA, MULTI_OPCION, COLABORADOR, TEXTO_LARGO, DECIMAL) + `FECHA` con flag `generaAlerta` y offsets propios (D1, mÃ¡s flexible que el tipo aparte de D4) + de D4: `seccion` del menÃº, `vinculo GLOBAL|POR_COLABORADOR|POR_SEDE` (reemplaza el booleano `llevaSede` de D1; el vÃ­nculo por colaborador es necesario para casos como "memorandos"), wizard con dnd-kit, `mostrarEnTabla`, y mini-matriz de permisos `custom:{slug}` por rol. Campos `ARCHIVO`â†’`Documento`, `COLABORADOR`â†’uuid (D1). |
| C17 | **Crons**: uno solo (`/api/cron/alertas`, D1Â·D4) vs dos (`deadline-alerts` + `legal-calendar-horizon`, D3) | 1Â·3Â·4 | **Dos crons (D3)**: `/api/cron/calendario-legal` 10:30 UTC (genera ocurrencias con horizonte 120 dÃ­as) y `/api/cron/alertas` 11:00 UTC = 6:00 a.m. BogotÃ¡ (marca vencidos, despacha alertas). La separaciÃ³n garantiza que las ocurrencias del dÃ­a existan antes del barrido y aÃ­sla fallos. Ambos protegidos con `CRON_SECRET`, presupuesto 80% de `maxDuration` y self-reinvoke. Las instancias de dotaciÃ³n (30-abr/31-ago/20-dic) y exÃ¡menes periÃ³dicos que D1 metÃ­a en el cron de alertas se publican desde sus mÃ³dulos vÃ­a `publicarVencimiento()` o desde el cron de horizonte â€” nunca desde el cron de despacho. |
| C18 | **Notificaciones/correo**: `Notificacion + NotificacionEnvio` (D1) vs `Notification + OutboundMessage` outbox con dedupe y Resend Batch (D3) vs `NotificationChannel` interface (D4) | 1Â·3Â·4 | **D3**: `Notificacion` (in-app, `dedupeKey` Ãºnico) + `MensajeSaliente` (outbox por canal con `dedupeKey`, estados QUEUED/SENT/FAILED/DEAD) + registry de proveedores (`ResendEmailProvider` activo, WhatsApp por variable de entorno). Se **elimina** `NotificacionEnvio` de D1 (redundante con el outbox). Correos: crÃ­tico individual + digest diario agrupado por responsable, Resend Batch â‰¤100. La interface de D4 se funde con la de D3 (misma idea). |
| C19 | **Campana en tiempo real**: D3 propone Supabase Realtime con RLS `auth.uid()` â€” **incompatible con el veredicto C2** (Better Auth: no hay `auth.uid()` en las sesiones) | 2Â°-orden | **Polling**: refetch del contador cada 60 s + al recuperar foco de pestaÃ±a (PWA). Realtime con token custom queda como mejora futura. Este conflicto solo emerge al combinar diseÃ±os; hay que documentarlo para que nadie implemente la suscripciÃ³n Realtime de D3 tal cual. |
| C20 | **Calendario legal â€” quÃ© es obligaciÃ³n y quÃ© no**: D1 mete pÃ³lizas/arriendos/convenios/marca/dominios/licencias como `ObligacionLegal` (categorÃ­as `ARRIENDOS_POLIZAS`, `CONVENIOS_FINANCIERAS`) **y ademÃ¡s** como `DocumentoLegal` con `vigenciaFin` (doble alerta); D3 los excluye explÃ­citamente del calendario | 1Â·3 | **D3**: el calendario legal solo modela obligaciones recurrentes "de empresa". PÃ³lizas, arriendos, convenios financieras (Addi, PayJoyâ€¦), marca SIC, dominios y licencias SaaS se registran como `DocumentoLegal` con `vigenciaFin` â†’ publican su `Vencimiento` tipado (`poliza`, `arriendo`, `convenio_financiera`, `marca`, `dominio_web`, `licencia_software`). Una sola fuente de alerta por cosa. El motor de recurrencias de D3 (`EVERY_N_YEARS`, `EVENT_BASED`, `SEMIANNUAL` con `dayPerMonth`, flag `usesTaxCalendar` para fechas segÃºn dÃ­gito del NIT, generaciÃ³n al completar + horizonte) reemplaza el `reglaVencimiento Json` ambiguo de D1. El seed une las dos listas (las 29 filas de D3 cubren todas las de D1). |
| C21 | **PrÃ©stamos**: `Prestamo/CuotaPrestamo` (D1) vs `Prestamo/PrestamoCuota` + reglas (D2) | 1Â·2 | Nombres D1 (`CuotaPrestamo`) + reglas de negocio D2: autorizaciÃ³n escrita obligatoria (Documento, CST 149), protecciÃ³n del neto (deducciones â‰¤ 50% â†’ cuota parcial + reprogramaciÃ³n al final del plan + notificaciÃ³n a NÃ³mina), saldo al retiro ofrecido en liquidaciÃ³n definitiva y enlazado al Ã­tem de cartera del paz y salvo. |
| C22 | **Clase de riesgo ARL**: por colaborador (D1) vs por cargo/centro de trabajo (D2) | 1Â·2 | **Por cargo** (`Cargo.claseRiesgoDefecto`) con **override por colaborador** â€” la clase de riesgo es legalmente por actividad/centro de trabajo (D2 tiene razÃ³n); el override cubre excepciones. `TarifaArl` con vigencias (D2), no tabla estÃ¡tica (D1). |
| C23 | **Comisiones**: solo novedad `Comision` (D1) vs `ReglaComision` + `NovedadComision` con import CSV (D2) | 1Â·2 | Ambas: `ReglaComision` (configuraciÃ³n % o tramos, por sede) + `Comision` (novedad con `baseCalculo`, periodo de causaciÃ³n â€” el recaudo se liquida cuando se confirma). Entrada por CSV o digitaciÃ³n (D2). Siempre constitutivas (CST 127). |
| C24 | **Versionado de documentos**: `VersionDocumentoLegal` (D1) **y** `Documento.version Int` (D4) â€” dos mecanismos | 1Â·4 | Solo `VersionDocumentoLegal` para el repositorio legal (RIT, polÃ­ticas, SG-SST) con Ã­ndice parcial "Ãºnica vigente". Se **elimina** `Documento.version`: los adjuntos operativos se reemplazan y la auditorÃ­a conserva el rastro. `TipoDocumento` (nombre final, fusiona `TipoDocumentoArchivo` D1 y `TipoDocumento` D4): `nivelAcceso` enum (subsume el booleano `sensible` de D4) + `requiereVencimiento` + override de dÃ­as de alerta (D4). Se conserva `DocumentoRequerido` de D1 (Ãºnico que soporta el semÃ¡foro documental por tipo de vÃ­nculo). |
| C25 | **Importador Excel**: SheetJS desde CDN propio (D4) vs sin definir (D1) | 1Â·4 | **`exceljs`** (npm puro). El tarball de cdn.sheetjs.com complica CI/Vercel y la versiÃ³n npm de `xlsx` tiene CVEs (lo seÃ±ala el propio D4). exceljs lee xlsx/CSV y escribe plantillas con validaciÃ³n de listas. Flujo D4 (parseo en cliente + validaciÃ³n zod compartida + lotes de 100 en transacciÃ³n) + registro `ImportacionDatos` de D1 con el archivo original en Storage para trazabilidad. |
| C26 | **Estructura de carpetas**: `src/lib/deadlines`, `src/lib/legal-calendar` (D3) vs `server/services/*` (D4) | 3Â·4 | **Estructura D4 manda** (es el diseÃ±o de arquitectura). Los archivos de D3 se reubican: `server/services/alertas/` (servicio, scheduler, cron-job), `server/services/calendario-legal/`, `lib/dias-habiles.ts`, `server/services/notificaciones/` (registry/dispatcher/providers), `src/emails/`. |
| C27 | **Estados de cuenta de cobro y de solicitud** (enums distintos D1/D2/D4) | varios | Cuenta de cobro: `RADICADA | EN_VERIFICACION_SS | BLOQUEADA_SS | APROBADA | PAGADA | RECHAZADA`. Solicitud: estados genÃ©ricos de D1 (`PENDIENTE/EN_APROBACION/APROBADA/RECHAZADA/CANCELADA`) + pasos; los estados `PENDIENTE_JEFE/PENDIENTE_RRHH` de D4 se derivan del paso activo (no se persisten). Tipos de solicitud: superset D1 (`VACACIONES, PERMISO, LICENCIA, CERTIFICACION_LABORAL, OTRA`). |
| C28 | **Bucket de Storage**: `documentos` Ãºnico (D1Â·D4) vs `nomina/desprendibles/...` (D2) | 1Â·2Â·4 | Un solo bucket privado `documentos` con prefijos por entidad (`nomina/desprendibles/{colaboradorId}/...` es un prefijo, no un bucket). Todo PDF generado se registra ademÃ¡s como `Documento`. Subida directa con signed upload URL (D4, evita lÃ­mite 4.5 MB de Vercel). |
| C29 | **Defaults de alerta** ("Ãºltima alerta 3 dÃ­as": Â¿hÃ¡biles o calendario?) | 1Â·3 | Ambos pasos del default global en **dÃ­as hÃ¡biles** (D-10H, D-3H) â€” coincide D1 (`enDiasHabiles=true`) y D3 (`business:true`). Calendario legal: D-5H + D-1 calendario (literal del documento). Todo editable por tipo en `/configuracion/alertas`. |
| C30 | **PDF**: `@react-pdf/renderer` | 2Â·4 | Sin conflicto â€” ambos coinciden. Se ratifica (runtime nodejs, fuentes registradas, pdf-lib solo para unir expedientes). |

---

## 2. VACÃOS (requerimientos sin cubrir o cubiertos a medias) Y CÃ“MO CERRARLOS

Recorrido punto por punto del documento. Los Ã­tems que el usuario sugiriÃ³ verificar (dÃ­a de la familia, evaluaciÃ³n de desempeÃ±o, profesiograma, plan de emergencias, canal anti-acoso, RUT contratista, foto, organigrama, modalidad, importador) **sÃ­ estÃ¡n cubiertos** por al menos un diseÃ±o â€” los vacÃ­os reales son estos:

| # | VacÃ­o | Estado actual | Propuesta de cobertura |
|---|---|---|---|
| V1 | **SuspensiÃ³n del contrato** (sanciÃ³n disciplinaria `SUSPENSION`, licencia no remunerada larga, fuerza mayor) con efectos en nÃ³mina | D1 tiene `EstadoContrato.SUSPENDIDO` y el resultado disciplinario, y D2 menciona "suspensiones" en el pseudocÃ³digo, pero **ningÃºn diseÃ±o la modela** | Modelo `SuspensionContrato(contratoId, fechaInicio, fechaFin, causa, origenTipo/origenId)` â€” alimentada por el disciplinario o manual. Efectos: descuenta dÃ­as en liquidaciÃ³n, genera novedad SLN en el Resumen PILA, descuenta de la base de vacaciones/cesantÃ­as segÃºn causa (CST art. 51-53). |
| V2 | **Libranzas y embargos judiciales** | D2 los lista como conceptos de deducciÃ³n (`LIBRANZA`, `EMBARGO`) pero **no existe modelo** que los gestione (solo `Prestamo` interno) | Modelo `OrdenDescuento(colaboradorId, tipo LIBRANZA\|EMBARGO\|OTRO, beneficiario, cuentaDestino, valorFijo o porcentaje, prioridad, fechaInicio/Fin, soporteâ†’Documento)`. El motor aplica topes de inembargabilidad (regla general: solo lo que excede el SMMLV, 1/5; alimentos hasta 50%; cooperativas 50%) y orden de prelaciÃ³n antes de la regla del 50% del neto. |
| V3 | **Preaviso de no renovaciÃ³n de contrato fijo (30 dÃ­as, CST art. 46)** | Las alertas avisan del vencimiento, pero ninguna modela la decisiÃ³n de renovar/no renovar ni el plazo legal del preaviso | Paso extra en la regla de alerta del tipo `contrato_fijo` (offset D-45) + acciÃ³n "Registrar decisiÃ³n" en el detalle del contrato: renovar (crea `ProrrogaContrato`) o no renovar (genera carta de preaviso desde `PlantillaDocumento`, valida que falten â‰¥30 dÃ­as, crea la `Terminacion` tipo `VENCIMIENTO_PLAZO`). |
| V4 | **DÃ­a de la Familia como obligaciÃ³n, no solo como licencia** (Ley 1857/2017: una jornada semestral) | D1 solo tiene `TipoLicencia.DIA_DE_LA_FAMILIA` (el registro individual) | ObligaciÃ³n seed `SEMIANNUAL` "Jornada DÃ­a de la Familia" en el calendario legal + reporte de cobertura (colaboradores con/sin registro en el semestre) + registro masivo (seleccionar asistentes, crea las licencias en lote). |
| V5 | **Acuse de recibo de RIT y polÃ­ticas por colaborador** | El repositorio versiona documentos, pero el debido proceso disciplinario exige probar que el colaborador **conoce** el RIT; nadie lo modelÃ³ | Modelo `AcuseDocumento(versionDocumentoLegalId, colaboradorId, fechaAcuse, medio)` + pantalla en autoservicio "Documentos por aceptar" + bloqueo blando en el disciplinario si no hay acuse del RIT vigente. |
| V6 | **VigÃ­a vs COPASST segÃºn headcount y nivel de estÃ¡ndares Res. 0312 (7/21/60 Ã­tems)** | D1 tiene los tipos de comitÃ© y la autoevaluaciÃ³n con `detalleItems Json`, pero ningÃºn diseÃ±o define la lÃ³gica por tamaÃ±o de empresa | Regla en el mÃ³dulo SST: con headcount activo (+ clase de riesgo) sugerir VigÃ­a (<10) o COPASST (â‰¥10) y precargar el checklist de autoevaluaciÃ³n del nivel correspondiente (3 plantillas seed: 7/21/60 estÃ¡ndares). |
| V7 | **Trazabilidad individual de consignaciÃ³n de cesantÃ­as** | El calendario tiene la obligaciÃ³n global del 14-feb, pero no hay registro por colaborador de cuÃ¡nto se consignÃ³ y a quÃ© fondo | Tabla `ConsignacionCesantias(colaboradorId, anio, fondoId, valor, fechaConsignacion, soporteâ†’Documento)` generada desde la provisiÃ³n acumulada del aÃ±o, con reporte de cierre y diferencia provisiÃ³n vs consignado. |
| V8 | **Elegibilidad de dotaciÃ³n (â‰¤2 SMMLV y â‰¥3 meses de antigÃ¼edad, CST 230)** | Los diseÃ±os registran la entrega pero no quiÃ©n tiene derecho | ValidaciÃ³n + reporte "elegibles por corte" antes de cada entrega (30-abr/31-ago/20-dic), usando `resolverParametro('SMMLV', fechaCorte)` y `fechaIngreso`. |
| V9 | **MigraciÃ³n de saldos iniciales en el importador** (vacaciones acumuladas, contrato vigente, salario actual) | D1 tiene `AjusteVacaciones` "para migraciÃ³n" y D4 el wizard, pero la plantilla solo crea la ficha | Extender la plantilla Excel con columnas: salario actual, tipo/fechas de contrato, dÃ­as de vacaciones pendientes â†’ la importaciÃ³n crea `Contrato` ACTIVO + `AjusteVacaciones` inicial en la misma transacciÃ³n. Sin esto, la primera nÃ³mina y el saldo de vacaciones nacen mal. |
| V10 | **Campos para WhatsApp "preparado"** | D3 asume `phone_e164` y `whatsapp_opt_in` en el perfil; ningÃºn modelo los define | AÃ±adir `telefonoE164` y `whatsappOptIn` (consentimiento Ley 1581 con fecha) al `User`/`Colaborador` desde la fase 1, para que activar el canal no requiera migraciÃ³n. |
| V11 | **Simulacro anual de emergencias** | "Plan de emergencias" quedÃ³ solo como documento (`DocumentoLegal` subtipo) | ObligaciÃ³n SST anual seed "Simulacro de evacuaciÃ³n por sede" (`perBranch=true`) con acta como evidencia. |
| V12 | **Plan de mejora de la autoevaluaciÃ³n SST como acciones rastreables** | D1 lo deja como "Documento adjunto" aunque `AccionCorrectiva` ya acepta `origenTipo=AutoevaluacionSst` | Formalizar: al registrar autoevaluaciÃ³n con puntaje <100, exigir creaciÃ³n de `AccionCorrectiva` por Ã­tem incumplido (cada una publica su `Vencimiento`). |
| V13 | **Export contable de nÃ³mina (rol Contador)** | D2 puso `cuentaContable` en `ConceptoNomina` pero nadie diseÃ±Ã³ el reporte | Reporte `/reportes/contabilizacion-nomina`: agregado por concepto/cuenta contable/sede por periodo, export xlsx. Sin interfaz a software contable en v1. |
| V14 | **Contenido del dashboard de inicio por rol** | D4 lo nombra ("dashboard por rol") sin definirlo | Definir widgets mÃ­nimos: Admin/RRHH (vencimientos rojos/amarillos, headcount, solicitudes por aprobar, cumpleaÃ±os), NÃ³mina (estado del periodo, novedades pendientes), Jefe (su equipo, aprobaciones), Empleado (saldo vacaciones, Ãºltimo desprendible, solicitudes), SST (semÃ¡foro, accidentes del mes), JurÃ­dica (obligaciones prÃ³ximas, disciplinarios abiertos). |
| V15 | **Practicantes universitarios â‰  aprendices SENA** | D1 tiene el `tipoVinculo PRACTICANTE` y D2 solo trata aprendices SENA | Definir reglas v1: practicante con apoyo de sostenimiento pactado, afiliaciÃ³n ARL pagada por la empresa, sin prestaciones; conceptos de nÃ³mina restringidos igual que aprendiz, con parÃ¡metro propio. |
| V16 | **Headcount y horas-hombre automÃ¡ticos para indicadores SST** | `DatoMensualSst` es de captura manual en D1 | El cron mensual lo precalcula (colaboradores activos del mes Ã— jornada vigente âˆ’ ausentismos) con posibilidad de override manual auditado. |
| V17 | **NÃ³mina electrÃ³nica DIAN** â€” *no estÃ¡ en el documento de requerimientos*, pero KUPOCELL casi con certeza estÃ¡ obligada | NingÃºn diseÃ±o la menciona | SeÃ±alar al cliente: fuera de alcance v1; el modelo de `DetalleNomina` con snapshot contiene los datos necesarios; integraciÃ³n futura vÃ­a proveedor tecnolÃ³gico autorizado. DecisiÃ³n explÃ­cita, no omisiÃ³n. |
| V18 | **DispersiÃ³n bancaria del pago de nÃ³mina** | `Banco.codigoAch` existe (D1) pero nadie genera el archivo | Opcional/confirmar con cliente: export xlsx/CSV plano por banco con cuenta+valor neto por colaborador desde el periodo `APROBADA`. Bajo costo, alto valor operativo. |

---

## 3. ORDEN DE IMPLEMENTACIÃ“N (fases incrementales, cada una desplegable y verificable)

**Fase 1 â€” Esqueleto desplegable: auth + shell + fundaciones (2-3 semanas)**
- Construye: proyecto Next.js 15 + Prisma + Supabase PG (pooler + directUrl), Better Auth (login, invitaciÃ³n por Resend, cambio de contraseÃ±a forzado, recuperaciÃ³n), shell responsive (sidebar desktop / bottom-nav + drawer mÃ³vil), PWA base (manifest + Serwist, pÃ¡gina /offline), selector de sede (cookie), extensiÃ³n Prisma de auditorÃ­a + AsyncLocalStorage, `requirePermission/scopeWhere`, seed de 9 roles + matriz, CRUD sedes/ciudades, `ConfiguracionEmpresa`, pantalla usuarios/roles.
- Entidades: `User/Session/Account`, `Rol`, `RolPermiso`, `UsuarioSede`, `Ciudad`, `Sede`, `ConfiguracionEmpresa`, `AuditLog`.
- VerificaciÃ³n: deploy en Vercel; login con admin seed; crear usuario por invitaciÃ³n (llega correo, fuerza cambio de contraseÃ±a); editar matriz de permisos y ver el menÃº cambiar por rol; `AuditLog` registra el alta con diff; instalar la PWA en un celular; selector de sede persiste.

**Fase 2 â€” Colaboradores + gestiÃ³n documental + importador (3-4 semanas)**
- Construye: ficha completa con tabs (datos, educaciÃ³n, bancarios, salud-afiliaciones, documentos, histÃ³rico de auditorÃ­a), catÃ¡logos (`EntidadSeguridadSocial`, `Banco`, `Area`, `Cargo`), `Documento` polimÃ³rfico + `TipoDocumento` + `DocumentoRequerido` (semÃ¡foro documental), subida directa a Storage (signed upload URL, cÃ¡mara en mÃ³vil, compresiÃ³n), visor, organigrama (d3-org-chart), bÃºsqueda global (Ctrl+K, pg_trgm), importador Excel con saldos iniciales (V9).
- Entidades: `Colaborador`, `EducacionColaborador`, `Area`, `Cargo`, `EntidadSeguridadSocial`, `Banco`, `TipoDocumento`, `DocumentoRequerido`, `Documento`, `ImportacionDatos`, `AjusteVacaciones`.
- VerificaciÃ³n: crear colaborador completo con foto y 5 documentos desde el celular; semÃ¡foro documental muestra faltantes segÃºn vÃ­nculo; importar Excel de 100 filas con 7 errÃ³neas â†’ 93 insertadas + reporte de errores; organigrama navegable; buscar por nombre/cÃ©dula respeta alcance del rol.

**Fase 3 â€” Motor de vencimientos + notificaciones (2-3 semanas) â€” antes que cualquier mÃ³dulo que dependa de alertas**
- Construye: `lib/dias-habiles.ts` (colombian-holidays + `FestivoExcepcion` + config sÃ¡bado, con tests), `publicarVencimiento/resolverVencimiento/cancelarVencimiento`, materializaciÃ³n de `AlertaVencimiento`, `ReglaAlerta` + editor en configuraciÃ³n, cron de alertas (claim SKIP LOCKED, outbox, Resend Batch, digest + crÃ­tico), `Notificacion` + campana (polling), tablero `/vencimientos` con semÃ¡foro y filtros sede/ciudad/dominio. Primera fuente: `Documento.fechaVencimiento`.
- Entidades: `TipoVencimiento`, `Vencimiento`, `ResponsableVencimiento`, `ReglaAlerta`, `AlertaVencimiento`, `Notificacion`, `MensajeSaliente`, `FestivoExcepcion`.
- VerificaciÃ³n: subir documento que vence en 12 dÃ­as hÃ¡biles â†’ se materializan pasos D-10H/D-3H/DUE; simular fecha (parÃ¡metro de test del cron) â†’ llegan in-app + correo digest; ejecutar el cron dos veces seguidas â†’ cero duplicados; test unitario de festivos (traslado Emiliani 2026); resolver el documento â†’ alertas futuras SKIPPED.

**Fase 4 â€” ContrataciÃ³n: laborales + OPS + cuentas de cobro (3-4 semanas)**
- Construye: pestaÃ±as Fijo/Indefinido/Obra/Aprendizaje, prÃ³rrogas (reglas CST: 3 prÃ³rrogas <1 aÃ±o, mÃ¡x 4 aÃ±os), otrosÃ­ con valores antes/despuÃ©s que actualiza snapshots, periodo de prueba â†’ vencimiento, `PlantillaDocumento` con variables y render, OPS (objeto/valor/supervisor/entregables/RUT), cuentas de cobro con `SoporteSsOps` + bloqueo SS + CHECK en BD, preaviso de no renovaciÃ³n (V3), `VariacionSalarial`.
- Entidades: `Contrato`, `ProrrogaContrato`, `OtrosiContrato`, `ContratoOps`, `EntregableOps`, `CuentaCobroOps`, `SoporteSsOps`, `PlantillaDocumento`, `VariacionSalarial`, `SuspensionContrato` (V1).
- VerificaciÃ³n: contrato fijo publica vencimiento; prÃ³rroga lo reprograma (alertas regeneradas); otrosÃ­ de sede actualiza `Colaborador.sedeId` y deja histÃ³rico; intentar aprobar cuenta de cobro sin soporte SS â†’ bloqueada (UI y constraint); reporte "cuentas sin soporte SS" con datos reales.

**Fase 5 â€” Novedades + autoservicio + aprobaciones + certificaciones (3 semanas)**
- Construye: incapacidades (con prÃ³rrogas y recobro), licencias, permisos, vacaciones (solicitudâ†’aprobaciÃ³nâ†’disfrute, vista `v_saldo_vacaciones`), bonificaciones (estado pago, constitutivo, soporte), flujo de aprobaciÃ³n configurable + bandeja "por aprobar", certificaciones laborales 4 tipos con **primer PDF** (`@react-pdf/renderer` + membrete), descarga en autoservicio.
- Entidades: `Incapacidad`, `Licencia`, `Permiso` (novedad), `Vacaciones`, `Bonificacion`, `Solicitud`, `PasoAprobacionSolicitud`, `FlujoAprobacion`, `PasoFlujoAprobacion`, `CertificacionLaboral`.
- VerificaciÃ³n: empleado solicita vacaciones desde el celular â†’ jefe aprueba â†’ RRHH aprueba â†’ registro creado y saldo descontado, notificaciones en cada paso; saldo de la vista coincide con cÃ¡lculo manual (15 dÃ­as/360 + ajuste inicial); certificado con salario generado en PDF, guardado y descargable.

**Fase 6 â€” NÃ³mina (5-6 semanas; la mÃ¡s compleja, ya con contratos y novedades reales)**
- Construye: seeds de parÃ¡metros 2025/2026 con vigencias y fuente legal, conceptos sistema + configurables (mathjs restringido), `NovedadHoras` + clasificador automÃ¡tico, comisiones, prÃ©stamos/cuotas + `OrdenDescuento` (V2), motor de liquidaciÃ³n (IBC + Ley 1393, FSP, exoneraciÃ³n 114-1, incapacidades por tramo, aux. transporte automÃ¡tico, retefuente proc. 1), aprobaciÃ³n y cierre con snapshot, desprendibles PDF en lote, Resumen PILA xlsx, export contable (V13).
- Entidades: `ParametroLegal`, `TarifaArl`, `RangoFsp`, `TramoRetefuente`, `TipoHora`, `ConceptoNomina`, `PeriodoNomina`, `LiquidacionNomina`, `DetalleNomina`, `NovedadHoras`, `ReglaComision`, `Comision`, `Prestamo`, `CuotaPrestamo`, `OrdenDescuento`, `PlanillaPila`.
- VerificaciÃ³n: **suite de golden tests** validada con el contador: salario mÃ­nimo con aux. transporte; HE nocturna pre/post 25-dic-2025; dominical 80% (jun-2026) vs 90% (jul-2026); divisor 220â†’210 el 15-jul-2026; incapacidad 5 dÃ­as (2 empleador + 3 EPS); FSP en 4 SMMLV; exonerado vs no exonerado â‰¥10 SMMLV; ingreso a mitad de mes. Cuadre Î£ devengadosâˆ’deducciones=neto por periodo; liquidar dos veces el mismo periodo â†’ resultado idÃ©ntico (idempotencia); desprendible PDF correcto en mÃ³vil.

**Fase 7 â€” Terminaciones + liquidaciÃ³n definitiva + paz y salvo (2-3 semanas)**
- Construye: terminaciÃ³n por tipos, liquidaciÃ³n definitiva (cesantÃ­as/intereses/prima/vacaciones con promedios de variable, indemnizaciÃ³n CST 64), paz y salvo con checklist por Ã¡rea (Ã­tems automÃ¡ticos por activos asignados y prÃ©stamos con saldo), actas PDF, enlace a examen de egreso.
- Entidades: `Terminacion`, `LiquidacionDefinitiva`, `DetalleLiquidacionDefinitiva`, `PazYSalvo`, `PazYSalvoItem`, `PlantillaPazYSalvoItem`.
- VerificaciÃ³n: terminar empleado de prueba con comisiones â†’ liquidaciÃ³n coincide con cÃ¡lculo manual del contador; despido sin justa causa calcula indemnizaciÃ³n segÃºn antigÃ¼edad y rango salarial; paz y salvo no cierra con Ã­tems pendientes; bloqueo si hay periodos de nÃ³mina sin cerrar.

**Fase 8 â€” Activos, dotaciÃ³n, capacitaciones, evaluaciones (2 semanas)**
- Construye: inventario y asignaciÃ³n con actas PDF entrega/devoluciÃ³n, dotaciÃ³n 3Ã—aÃ±o con elegibilidad (V8) y vencimientos, capacitaciones con asistencia, evaluaciÃ³n de desempeÃ±o con plantillas.
- Entidades: `Activo`, `AsignacionActivo`, `EntregaDotacion`, `DetalleDotacion`, `Capacitacion`, `AsistenciaCapacitacion`, `PlantillaEvaluacion`, `EvaluacionDesempeno`.
- VerificaciÃ³n: asignar portÃ¡til â†’ acta PDF firmable; iniciar terminaciÃ³n con activo asignado â†’ Ã­tem automÃ¡tico en paz y salvo; corte de dotaciÃ³n lista solo elegibles.

**Fase 9 â€” JurÃ­dica completa + calendario legal (3-4 semanas)**
- Construye: repositorio legal con versionado y acuses (V5), procesos disciplinarios por etapas con actas, canal de denuncias anti-acoso (cÃ³digo anÃ³nimo, acceso restringido), habeas data (autorizaciones, consultas/reclamos con plazos), `ObligacionLegal` + ocurrencias + cron de horizonte + seed completo de 29+ obligaciones, UI calendario+lista, completar ocurrencia con evidencia.
- Entidades: `DocumentoLegal`, `VersionDocumentoLegal`, `AcuseDocumento`, `ProcesoDisciplinario`, `EtapaProcesoDisciplinario`, `DenunciaAcoso`, `ActuacionDenuncia`, `AutorizacionDatos`, `ConsultaReclamoDatos`, `ObligacionLegal`, `OcurrenciaObligacion`.
- VerificaciÃ³n: completar "PILA marzo" â†’ aparece "PILA abril" inmediatamente; obligaciÃ³n `perBranch` genera una ocurrencia por sede; alertas a 5 hÃ¡biles y 1 dÃ­a; denuncia anÃ³nima consultable por cÃ³digo; disciplinario no permite decisiÃ³n sin descargos; reporte semestral SIC con datos de consultas/reclamos.

**Fase 10 â€” SST (3-4 semanas)**
- Construye: comitÃ©s (sugerencia VigÃ­a/COPASST por headcount, V6) con reuniones/compromisos, IPEVR por sede (GTC-45), profesiograma por cargo, exÃ¡menes mÃ©dicos con prÃ³ximos segÃºn profesiograma, novedades ARL, accidentes (FURAT 2 dÃ­as hÃ¡biles, investigaciÃ³n 15 dÃ­as, acciones correctivas), inspecciones, EPP con reposiciÃ³n, autoevaluaciÃ³n 0312 por nivel + plan de mejora (V12), indicadores con headcount automÃ¡tico (V16), tablero SST.
- Entidades: `Comite`, `MiembroComite`, `ReunionComite`, `CompromisoReunion`, `PeligroIpevr`, `ProfesiogramaCargo`, `ExamenMedico`, `RecomendacionMedica`, `NovedadArl`, `AccidenteTrabajo`, `InvestigacionAccidente`, `AccionCorrectiva`, `InspeccionSst`, `ElementoEpp`, `EntregaEpp`, `DetalleEntregaEpp`, `AutoevaluacionSst`, `DatoMensualSst`, `IndicadorSst`.
- VerificaciÃ³n: registrar examen periÃ³dico â†’ prÃ³ximo vencimiento segÃºn profesiograma; accidente â†’ alerta FURAT a 2 dÃ­as hÃ¡biles; comitÃ© conformado â†’ vencimiento a 2 aÃ±os; IF/IS del mes coinciden con cÃ¡lculo manual; semÃ¡foro SST refleja documentos SG-SST vigentes; datos clÃ­nicos invisibles para rol NÃ³mina.

**Fase 11 â€” MÃ³dulos personalizados + reportes + cierre (2-3 semanas)**
- Construye: constructor de mÃ³dulos (wizard 3 pasos, dnd-kit, permisos `custom:{slug}`), `DynamicForm/DynamicTable`, campos FECHA con alerta, vistas SQL (`v_saldo_vacaciones` ya existe; `v_ausentismo`, `v_rotacion`, `v_masa_salarial`, `v_semaforo_documental`), catÃ¡logo `/reportes` con filtros sede/ciudad y export xlsx, dashboards por rol (V14), hardening PWA y pulido mÃ³vil.
- VerificaciÃ³n: Admin crea mÃ³dulo "PÃ³lizas de vehÃ­culos" con campo fecha-con-alerta sin deploy â†’ registro genera vencimiento y alerta; cada reporte del documento (personas por sede/cargo, laborales vs OPS, remotos, masa salarial, semÃ¡foro, fijos por vencer, fin de prueba, cuentas sin SS, rotaciÃ³n, ausentismo, accidentalidad) se valida contra los datos sembrados; export respeta filtros y alcance del rol.

Dependencias clave del orden: el motor de alertas (F3) va antes que contratos/jurÃ­dica/SST porque todos publican en Ã©l; nÃ³mina (F6) va despuÃ©s de contratos y novedades porque los consume; terminaciones (F7) despuÃ©s de nÃ³mina porque la liquidaciÃ³n definitiva exige periodos cerrados.

---

## 4. RIESGOS TÃ‰CNICOS Y MITIGACIÃ“N

| # | Riesgo | MitigaciÃ³n |
|---|---|---|
| R1 | **LÃ­mites de Vercel Cron/serverless**: en plan Hobby `maxDuration` efectivo es muy bajo y solo crons diarios; en Pro 300 s (hasta 800 s con Fluid). Despacho de cientos de alertas + render de correos puede exceder | Plan **Pro** desde el inicio. Presupuesto de tiempo al 80% + `selfReinvoke` (el estado vive en BD: alertas PENDING restantes). Lotes de 200 con claim `FOR UPDATE SKIP LOCKED`. `scheduled_for <= hoy` (no `=`) hace catch-up automÃ¡tico si un dÃ­a falla. GeneraciÃ³n de desprendibles PDF en lotes encolados, no en una sola invocaciÃ³n. |
| R2 | **Supabase free tier**: 500 MB de BD, 1 GB de Storage, sin PITR â€” incompatible con "muchos documentos escaneados por empleado" y "respaldo en la nube" | Plan **Pro de Supabase** ($25/mes: 8 GB BD, 100 GB Storage, backups diarios 7 dÃ­as; PITR como add-on). CompresiÃ³n de imÃ¡genes en cliente (~1600px/80%) antes de subir; lÃ­mite 25 MB/archivo; monitoreo de uso en el dashboard de configuraciÃ³n. Documentar que el free tier solo sirve para desarrollo. |
| R3 | **PrecisiÃ³n decimal en nÃ³mina**: floats de JS corrompen centavos; Prisma `Decimal` no se serializa a Client Components | `Decimal(14,2)` + decimal.js en TODO el motor; jamÃ¡s `Number` en cÃ¡lculos. Redondeo al peso `ROUND_HALF_UP` por concepto; `round100up` SOLO en aportes/PILA. Serializar Decimal como string en el lÃ­mite Serverâ†’Client. Golden tests con valores exactos validados por el contador como gate de la fase 6. |
| R4 | **Zona horaria America/Bogota**: el cron corre en UTC (las 00:00â€“05:00 UTC son "ayer" en BogotÃ¡); `new Date('2026-06-11')` se parsea UTC en server y local en browser â†’ fechas corridas un dÃ­a | Toda fecha de negocio es `DATE` puro (sin hora). Helper Ãºnico `todayBogota()` con `Intl` (offset -5 fijo, sin DST). Prohibido `new Date(string)` para fechas puras: parser/formatter propios en `lib/`. Cron a las 11:00 UTC (06:00 BogotÃ¡). Tests de borde: ejecuciÃ³n a las 04:59 UTC debe seguir siendo "ayer" BogotÃ¡. |
| R5 | **SMMLV 2026 suspendido judicialmente** (Consejo de Estado feb-2026; Decreto 0159/2026 transitorio): posible recÃ¡lculo retroactivo masivo | ParÃ¡metros con vigencias editables por admin (C7) + snapshot por periodo + reliquidaciÃ³n que genera **periodo de ajuste** (nunca muta cerrados). Probar este flujo en los golden tests de la fase 6, no improvisarlo cuando llegue el fallo. |
| R6 | **Prisma + PgBouncer (pooler de Supabase)**: prepared statements y transacciones interactivas fallan en transaction mode | `DATABASE_URL` con `pgbouncer=true` + `DIRECT_URL` para migraciones (ya en D1). Transacciones de liquidaciÃ³n de nÃ³mina por lotes cortos, no una transacciÃ³n interactiva de minutos. |
| R7 | **Incompatibilidad Better Auth â†” Supabase Realtime/RLS** (conflicto C19): el diseÃ±o 3 asumÃ­a `auth.uid()` | Polling para la campana (60 s + focus). Nada de RLS: autorizaciÃ³n 100% en capa de aplicaciÃ³n â€” documentarlo como decisiÃ³n para que nadie "active RLS" a medias despuÃ©s. |
| R8 | **AuditorÃ­a fire-and-forget**: el `void prisma.auditLog.create` de D4 puede perder registros si la lambda muere; el `findUnique` previo al update tiene carrera | Escribir el log **en la misma transacciÃ³n** de la mutaciÃ³n cuando exista (D1); fire-and-forget solo para lecturas/login. Lista de exclusiÃ³n: `AuditLog`, `Session`, `Verification`, `Notificacion`, `MensajeSaliente`, `AlertaVencimiento` (ruido). RedacciÃ³n de campos sensibles por modelo. |
| R9 | **mathjs como vector de inyecciÃ³n** en conceptos `EXPRESION` | Instancia `math.create()` restringida (solo aritmÃ©tica, whitelist de variables, sin strings/funciones); validar la fÃ³rmula al guardarla contra un scope de prueba; conceptos legales siempre `SISTEMA` en cÃ³digo versionado. |
| R10 | **@react-pdf/renderer en serverless**: fuentes y logo por request inflan memoria/latencia; 200 desprendibles en una invocaciÃ³n pueden agotar `maxDuration` | `Font.register` con fuentes locales del bundle, logo cacheado en mÃ³dulo, `runtime="nodejs"`. GeneraciÃ³n masiva de desprendibles como job por lotes (50 por invocaciÃ³n, re-invocaciÃ³n encadenada), estado en BD. |
| R11 | **Expectativa del archivo PILA**: el cliente puede asumir plano certificado Res. 2388 en v1 | Alcance pactado por escrito: v1 = Resumen Excel con totales de control; interfaz `PilaExporter` deja listo el v2. (Conflicto C12.) |
| R12 | **Dependencia `colombian-holidays` pequeÃ±a (~25 estrellas)** | Encapsulada en un solo archivo; `FestivoExcepcion` cubre dÃ­as decretados; el algoritmo Emiliani (~60 lÃ­neas) estÃ¡ documentado en D3 como plan B; test anual de los 18 festivos del aÃ±o siguiente en CI. |
| R13 | **LÃ­mites de Resend** (free: ~100/dÃ­a) vs digest diario a N responsables + crÃ­ticos individuales | Plan pago de Resend; Batch API â‰¤100/llamada; el patrÃ³n digest ya minimiza volumen (1 correo/usuario/dÃ­a + crÃ­ticos); outbox con reintentos (â‰¤5) y estado `DEAD` notificado al Admin; dominio `kupocell.com.co` verificado (SPF/DKIM) antes del go-live de la fase 3. |
| R14 | **LÃ­mite de 4.5 MB del body en Vercel** para subida de documentos | Ya resuelto por diseÃ±o: signed upload URL directo a Storage (D4); el servidor nunca recibe el binario. Verificarlo tambiÃ©n para el importador (parseo en cliente). |
| R15 | **Concurrencia en liquidaciÃ³n de nÃ³mina** (dos usuarios liquidan el mismo periodo) | Advisory lock de Postgres por `periodoId` + transiciÃ³n de estado `BORRADORâ†’CALCULADA` como guard; recÃ¡lculo siempre borra-e-inserta dentro de la transacciÃ³n (D2). |
| R16 | **UUIDv7 en Prisma** requiere versiÃ³n â‰¥5.19 y los Ã­ndices parciales/CHECKs van fuera del schema | Fijar versiÃ³n de Prisma en `package.json`; todos los CHECK/Ã­ndices parciales/vistas en migraciones SQL custom versionadas (lista consolidada del Â§12 de D1) con test de integridad que intenta violar cada constraint. |
| R17 | **Ley 1581 / datos sensibles**: diagnÃ³sticos, denuncias y cuentas bancarias | `nivelAcceso` en documentos + permiso `personas.salud` + redacciÃ³n en auditorÃ­a + URLs firmadas de 10 min + service worker con `NetworkOnly` para datos y Storage (nada sensible en Cache Storage) + acceso a denuncias restringido a ComitÃ© de Convivencia/JurÃ­dica. |

---

**Resumen ejecutivo del veredicto**: esquema en espaÃ±ol (D1) como base del modelo de datos; Better Auth (D4) para identidad; motor de alertas con la arquitectura de D3 (materializaciÃ³n + outbox + reglas configurables) renombrada al espaÃ±ol; nÃ³mina con el motor temporal de D2 (parÃ¡metros clave/valor con vigencias, clasificador de horas, Resumen PILA v1); estructura de carpetas y patrones transversales de D4; `colombian-holidays` como Ãºnica fuente de festivos para los tres dominios. 18 vacÃ­os identificados con propuesta concreta (los crÃ­ticos: suspensiÃ³n de contrato, libranzas/embargos, preaviso de no renovaciÃ³n, acuses de RIT y migraciÃ³n de saldos iniciales). 11 fases incrementales, cada una desplegable y con criterios de verificaciÃ³n explÃ­citos; gate de la fase de nÃ³mina = golden tests validados por el contador.
